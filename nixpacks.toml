[phases.setup]
nixPkgs = ["openssl", "openssl.dev", "pkg-config"]

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build",
  "npx prisma generate"
]

[phases.start]
cmd = "npm run start:railway"

[variables]
NODE_ENV = "production"
OPENSSL_LIB_DIR = "/nix/store/*/lib"
OPENSSL_INCLUDE_DIR = "/nix/store/*/include"
